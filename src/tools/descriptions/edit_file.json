{
  "name": "edit_file",
  "display_name": "编辑文件",
  "description": "Replace content of an existing UTF-8 file by an inclusive 1-based line range.\nUse read_file first to confirm the exact line numbers. For complex edits, multiple changes, or multiple edits to the same file, prefer apply_patch.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Workspace-relative or absolute file path."
      },
      "start_line": {
        "type": "integer",
        "description": "First line to replace, 1-based."
      },
      "end_line": {
        "type": "integer",
        "description": "Last line to replace, inclusive."
      },
      "replacement": {
        "type": "string",
        "description": "Replacement text; may contain multiple lines. Empty text deletes the specified line range."
      }
    },
    "required": [
      "path",
      "start_line",
      "end_line",
      "replacement"
    ]
  },
  "permission": "writes",
  "always_loaded": false,
  "load_policy": "group",
  "groups": [
    "dev"
  ]
}
