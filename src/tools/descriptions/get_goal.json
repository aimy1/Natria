{
  "name": "get_goal",
  "display_name": "读取目标",
  "description": "Read this session's current long-task goal: the exact goal_id/revision used for compare-and-set, the goal text, phase, used/max turn counts, blocking reason, and whether auto-continuation is armed. Must be called before update_goal to obtain the latest goal_id and revision.",
  "parameters": { "type": "object", "properties": {}, "additionalProperties": false },
  "permission": "reads",
  "always_loaded": false,
  "load_policy": "summary",
  "groups": ["planning"]
}
