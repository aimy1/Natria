{
  "name": "recall_memories",
  "display_name": "召回记忆",
  "description": "Search remembered knowledge points and past events; scope=episode searches only diary-style past events.\nTruncated entries in associative memory carry an id; use recall_memories id=<id> to fetch the full text. Use include_forgotten to include forgotten memories when needed. Read-only; does not change memory state.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search keywords or a question. May be omitted only when fetching directly by id."
      },
      "id": {
        "type": "integer",
        "description": "Fetch the full text of a single memory directly by id."
      },
      "scope": {
        "type": "string",
        "enum": [
          "all",
          "episode"
        ],
        "description": "all searches knowledge points and events (default); episode searches only past events."
      },
      "max_results": {
        "type": "integer",
        "description": "Optional limit on the number of results."
      },
      "include_forgotten": {
        "type": "boolean",
        "description": "Whether to include forgotten memories."
      }
    }
  },
  "permission": "readonly",
  "always_loaded": false,
  "load_policy": "summary",
  "groups": [
    "memory"
  ]
}
