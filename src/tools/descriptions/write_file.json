{
  "name": "write_file",
  "display_name": "写入文件",
  "description": "Write file contents. Creates the file if it does not exist, overwrites it if it does. Suited to creating new files or full rewrites; for complex partial edits prefer apply_patch.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "File path. Supports absolute paths, workspace-relative paths, and ~/ paths."
      },
      "content": {
        "type": "string",
        "description": "The complete file content to write."
      }
    },
    "required": [
      "path",
      "content"
    ]
  },
  "permission": "writes",
  "always_loaded": false,
  "load_policy": "group",
  "groups": [
    "dev"
  ]
}
