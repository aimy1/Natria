{
  "name": "read_artifact",
  "display_name": "读取预览文件",
  "description": "Read an existing file from the hosted Artifact workspace of the current WebUI session. Use the filename from the Artifact manifest; do not glob-search the hosted directory.",
  "parameters": {
    "type": "object",
    "properties": {
      "filename": {"type": "string", "description": "Hosted Artifact filename."},
      "offset": {"type": "integer", "description": "1-based line offset."},
      "limit": {"type": "integer", "description": "Maximum number of lines to read."}
    },
    "required": ["filename"],
    "additionalProperties": false
  },
  "always_loaded": true,
  "load_policy": "summary",
  "groups": ["dev"]
}
