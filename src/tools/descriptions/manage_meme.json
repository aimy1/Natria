{
  "name": "manage_meme",
  "display_name": "管理表情包",
  "description": "Maintain the current persona's writable meme library. action=add imports a local image, with metadata generated by the configured vision model when not provided; action=update edits metadata in the writable overlay; action=delete removes a user meme or disables a built-in one, and only hard_delete=true permanently deletes the file.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "add",
          "update",
          "delete"
        ],
        "description": "add imports, update edits metadata, delete removes."
      },
      "image": {
        "type": "string",
        "description": "add: local image path."
      },
      "id": {
        "type": "string",
        "description": "update/delete: full sha256 id or a unique short id."
      },
      "library": {
        "type": "string",
        "description": "Optional meme library override."
      },
      "name_zh": {
        "type": "string",
        "description": "Chinese name."
      },
      "name_en": {
        "type": "string",
        "description": "English name."
      },
      "description": {
        "type": "string",
        "description": "Description of the visible content."
      },
      "usage": {
        "type": "string",
        "description": "When to use it."
      },
      "avoid": {
        "type": "string",
        "description": "When not to use it."
      },
      "tags": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Search tags."
      },
      "enabled": {
        "type": "boolean",
        "description": "update: enable or disable."
      },
      "hard_delete": {
        "type": "boolean",
        "description": "delete: permanently delete instead of moving to the recycle bin."
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false
  },
  "always_loaded": false,
  "load_policy": "group",
  "groups": [
    "memes"
  ]
}
