{
  "name": "deep_research",
  "display_name": "深度研究",
  "description": "Run a deep research task and write out a Markdown report.\nResearcher and reviewer run as separate roles (dual-role); the final report is written to the configured output directory.",
  "parameters": {
    "type": "object",
    "properties": {
      "topic": {
        "type": "string",
        "description": "Research question or topic."
      },
      "thinking_depth": {
        "type": "string",
        "enum": [
          "minimal",
          "low",
          "medium",
          "high",
          "xhigh"
        ]
      }
    },
    "required": [
      "topic"
    ]
  },
  "permission": "readonly",
  "always_loaded": false,
  "load_policy": "group",
  "groups": [
    "research"
  ],
  "timeout_seconds": 0
}
